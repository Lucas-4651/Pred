<footer class="footer mt-5 py-4">
    <div class="container">
        <!-- Copyright avec style -->
        <div class="text-center">
            <p class="mb-3">
                <span class="footer-copyright">
                    &copy; <%= typeof copyright !== 'undefined' ? copyright : 'Lucas46Modder' %>
                </span>
                <span class="mx-2 text-white-50">|</span>
                <span class="footer-brand">VIRTUAL MG</span>
                <span class="mx-2 text-white-50">|</span>
                <span class="footer-version">v2.5.0</span>
            </p>
            
            <!-- Badges technos avec effet au survol -->
            <div class="d-flex justify-content-center gap-2 flex-wrap">
                <span class="tech-badge">⚡ Modèle VFL</span>
                <span class="tech-badge">🤖 Auto-learning</span>
                <span class="tech-badge">🎯 Score exact</span>
            </div>
            
            <!-- Petit message sympa (optionnel) -->
            <div class="mt-3">
                <small class="text-white-50">
                    <i class="fas fa-heart text-danger me-1"></i>
                    Pour but d'aider , Creer avec la passion
                </small>
            </div>
        </div>
    </div>
</footer>

<style>
.footer {
    position: relative;
    overflow: hidden;
}

/* Effet de fond subtil */
.footer::before {
    content: '';
    position: absolute;
    top: 0;
    left: -50%;
    width: 200%;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--madagascar-green), var(--madagascar-red), transparent);
    animation: scanline 8s linear infinite;
}

@keyframes scanline {
    0% { transform: translateX(-100%); }
    100% { transform: translateX(100%); }
}

/* Copyright avec soulignement animé */
.footer-copyright {
    color: #FFC107;
    font-weight: 500;
    position: relative;
    display: inline-block;
    cursor: default;
    padding-bottom: 2px;
}

.footer-copyright::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background: #FFC107;
    transition: width 0.3s ease;
}

.footer-copyright:hover::after {
    width: 100%;
}

/* Marque avec dégradé Madagascar */
.footer-brand {
    font-weight: 700;
    font-size: 1.1rem;
    background: linear-gradient(135deg, var(--madagascar-red), var(--madagascar-green));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    display: inline-block;
    transition: transform 0.3s ease;
}

.footer-brand:hover {
    transform: scale(1.05);
}

/* Version avec style monospace */
.footer-version {
    color: var(--madagascar-red);
    font-family: 'Courier New', monospace;
    font-weight: 600;
    padding: 2px 6px;
    border-radius: 4px;
    background: rgba(252, 61, 50, 0.1);
    transition: all 0.3s ease;
}

.footer-version:hover {
    background: rgba(252, 61, 50, 0.2);
    transform: translateY(-2px);
}

/* Badges technos stylisés */
.tech-badge {
    padding: 5px 12px;
    border-radius: 30px;
    font-size: 0.8rem;
    font-weight: 500;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: rgba(255, 255, 255, 0.8);
    transition: all 0.3s ease;
    cursor: default;
    backdrop-filter: blur(5px);
}

.tech-badge:hover {
    background: var(--madagascar-green);
    border-color: var(--madagascar-green);
    color: white;
    transform: translateY(-3px);
    box-shadow: 0 5px 15px rgba(0, 126, 58, 0.3);
}

/* Animation de pulse subtile sur le cœur */
.fa-heart {
    animation: heartBeat 1.5s ease infinite;
}

@keyframes heartBeat {
    0%, 100% { transform: scale(1); }
    25% { transform: scale(1.1); }
    50% { transform: scale(1); }
}

/* Responsive */
@media (max-width: 576px) {
    .footer-brand {
        font-size: 1rem;
    }
    
    .tech-badge {
        font-size: 0.7rem;
        padding: 4px 8px;
    }
}
</style>